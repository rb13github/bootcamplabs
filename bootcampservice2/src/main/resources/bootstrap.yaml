spring:
  application:
    name: conversionfactor-service
    
#  profiles:
#    active: ${profile:devlopment}
#    #  cloud:#    config:#      uri: ${configserver.url:http://localhost:8110}#       